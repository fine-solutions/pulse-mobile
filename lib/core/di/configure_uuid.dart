import 'package:uuid/uuid.dart';

Future<String> configureUuid() async {
  // TODO(KiSSEDBYFiR3): update uuid configuration after integraring auth feature

  // String uuid = await repository.getUuid();
  // if (uuid.isEmpty) {
  //   uuid = const Uuid().v4();
  //   // await repository.setUuid(uuid);
  // }
  return 'uuid';
}
